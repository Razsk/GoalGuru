# Local Web Stack with Google OAuth Authentication

Goal Guru is built as a local web application using Node.js/TypeScript and SQLite on the backend with a React frontend, secured via Google account OAuth authentication.

A local web application provides rapid cross-platform development without native desktop build toolchain dependencies. Integrating Google OAuth secures the user's local goal workspace, establishes identity for potential cloud sync or multi-device support, and avoids custom password handling.
